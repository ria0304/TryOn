# .env.example (frontend)
# URL of the FastAPI backend (see ./backend). Used both by Vite's dev-server
# proxy and, if set, directly by src/lib/api.ts at build time.
VITE_API_URL=http://localhost:8000
